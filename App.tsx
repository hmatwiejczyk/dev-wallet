import * as React from 'react';
import * as ReactDOM from 'react-dom';

import './styles/styles.scss';

const template = <p>hi there, new app is here!</p>;

ReactDOM.render(template, document.getElementById('app'));
